name: "Database Tables Report"
description: "List all tables in the database with metadata (row counts, size, columns)"
version: "1.0"
timeout: 120

parameters:
  - name: schema_name
    type: string
    required: false
    description: "Filter by specific schema (default: public)"
    default: "public"
